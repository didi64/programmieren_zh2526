notebookfinder
Uebungen

update -e
neu:Loesungen_07.ipynb

update 8: 

kurzer Blick auf Cheatsheat und Uebungen.
kurzer Blick auf Notebookfinder.

Recap 1: enumerate und zip, List-Komprehension, Files

sort,
Sortieren mit lambda-Ausdruecken, starte mit Beispiel.

names = [('Anna', 'Zaugg', 1),
         ('David',  'Zaugg', 4),
         ('David', 'Pfister', 2),
         ('Anna', 'Pfister', 3),
         ]

names.sort()

sorted(names)

# sortieren nach name[1] (name ist Tuple aus names)
sorted(names, key=x[1])


Mengen: Jedes Element nur einmal. Elemente ungeordnet.
        Kann rasch testen, ob Element in der Liste. Vorteil gegenueber Liste.
        Nachteil: Elemente sind ungeordnet. 
                  Kann mit For-Loop ueber Elemente iterieren, Reihenfolge ist zufaellig.

        Kann Mengen vereinen, schneiden und Differenz bilden.

numbers = set()
numbers

numbers.add(1)
numbers.add(2)
numbers.add(3)
numbers

for n in numbers:
    print(n)

numbers.remove(1)

n = numbers.pop()
n



numbers = set()
while len(numbers) < 7:
    n = randint(1, 9)
    numbers.add(n)

Schnitt: xs & ys
xs = {1, 2, 3}
ys = {2, 3, 4}
set(x for x in xs if x in ys)  # in x in xs und ys
=======

Dictionaries

Nachteil von Listen: um bestimmtes Element herausgreifen zu koennen, muss man den Index kennen.
Index finden kostet Zeit!

Anwendungen Dict

def is_triple(wurf):
    count_dict = make_count_dict(wurf)
    values = list(count_dict.values())
    return len(wurf) == 5 and 3 in values and 1 in values

def is_two_pairs(wurf):
    count_dict = make_count_dict(wurf)
    values = list(count_dict.values())
    return len(wurf) == 5 and values.count(2) == 2

Recap 2: Nicht nur Recap: Aufgenommen, was im Notebook Ortschaften der Lektion 7 begonnen.
        Beispiele fuer Listencomprehension in Action.
        Praktisches Arbeiten mit einem Datensatz.
        Ein nicht praezise formuliertes Problem loesen.
        So gut das eben moeglich ist.


