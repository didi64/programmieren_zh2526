Recap:
Index und Slicenotation:
Mit while Schleifen, machen Fkt einfacher mit FOr-Schleifen.
In String suchen und String spliten, kann der Strng selber,
Gibt sogenante Stringmethoden fuer diesen Zweck!

2 Tricks, die wir kennengelernt haben und oft gebraucht werden:
suffix,
tuple auspacken.


get_sep -> find: explain
split          : simplified: Wert un Wert abspalten, mit Tail weiterarbeiten

dann

get_idxs -> get_items
get_table

Review Whileschleifencode

For-Schleifen und die range-Funktion, aus While-Schleifen ausbrechen.
while Schleifen umschreiben.

get_items: einfacher, find: Variante, Geschacksache, was einfacher.


Nachtrag Funktionen, Signaturen, positionale Argumente und Keyword Argumente
keine grosse od. komplizierte Sache.
etwas kleines aber Nuetzliches



Funktionen mit Default-Argumenten
Bsp. Default-Argumente.

Kleine Aufgabe!
get_table(lines, indices, sep=',')

suffix = '.net'
email = example.com'
email[-len(suffix):] == suffix
review all code that we have written!

split Funktion, mit While-Schleifen,
Strings von Hand gesplittet.
Strings koennen sich selber in Tuple/Liste splitten

Spiele mit Stringmethoden, probiere einige aus.
Finde heraus, was sie tun!

Welche Zeichen sind isdigit?
chars = ''
for code_point in range(n):
    char = chr(code_point)
    if method(char):
        chars = chars + char

Listen und einige Listmethoden







